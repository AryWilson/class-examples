#include <stdio.h>

int main(){
  int value = 0;
  printf("enter a value :");
  scanf(" %i",&value);

  unsigned int mask = 10000;
  int result = mask & value;
  printf("\n value = %i, mask = %i, out= %i\n"val);

}
